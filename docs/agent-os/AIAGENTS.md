# AI AGENTS

Same OS as [AGENTS.md](./AGENTS.md). For coding agents in this repo.

## Scope

- Product code lives in this GitHub repo (`Josefusan/mygrokflow`).
- Agent OS docs live under `docs/agent-os/`. Do not implement product UI from a docs-only PR.
- Visual lock: [docs/VISUAL.md](../VISUAL.md). Feed column `max-w-[42rem]`, zinc B/W, tokens there.
- Pricing layout is in flight: catalog wants 72rem 4-up; production currently 56rem 3-up cards. Do not swap prices or layout unless founder names both together.

## Coding defaults

Load skills as named: `implement`, `tdd`, `prototype`, `domain-modeling`, `to-spec`, `to-tickets`, `code-review`, `diagnosing-bugs`, `karpathy-coding`, `frontend-ui`, `shadcn-discovery`, `shadcn-review`, `stripe-checkout-lanes`, `vercel-ship`, `apply-qualify-path`.

## Never

- Invent clients, metrics, vendors.
- Add unused shadcn primitives (allowed: button, card, badge, separator).
- Wire newsletter, Whop, Telegram apply-notify, or X sends without founder.
- Merge or deploy without founder/CEO.
